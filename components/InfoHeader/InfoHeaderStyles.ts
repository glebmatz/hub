import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    marginTop: 20,
  },
  backButton: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 15,
  },
});
